# Practical TODO

- Improve adaptive compression for videos that are too large to send through WhatsApp.
- Render QR data as a bitmap when an external client emits QR content.
- Validate `YouTubeApiClient` on a real device with a configured API key.
- Add Room tests with an in-memory database for chat sessions, jobs, and queue recovery.
- Persist runtime status for accurate statistics after app restarts.
